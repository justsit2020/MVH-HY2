# MVH-HY2
Suitable for setting up hy2 virtual hosting.
`server.jar` is used for Java-enabled virtual hosts, allowing the execution of any non-root command via `commands.txt`. `index.js` and `package.json` are suitable for virtual hosts that support Node.js. Modify the port number in `index.js` as needed.
